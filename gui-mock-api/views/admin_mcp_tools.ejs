<%
  const title = `MCP Tools · ${s.name}`;
  const body = include('partials/admin_mcp_tools_body', {
    s,
    tools,
    endpoints,
    query,
  });
%>
<%- include('layout', { title, body, adminKey: query?.key }) %>
