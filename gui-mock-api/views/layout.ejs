<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= title || 'GUI Mock API' %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
    <style>
      :root {
        --brand-primary: #0d6efd;
        --brand-secondary: #6610f2;
        --surface-color: #ffffff;
        --surface-border: rgba(13, 110, 253, 0.12);
        --surface-shadow: 0 1.5rem 3.5rem rgba(13, 110, 253, 0.08);
        --muted-color: #4c566a;
        --muted-border-color: rgba(13, 110, 253, 0.12);
        --danger-color: #b02a37;
      }

      body {
        background: linear-gradient(180deg, #f4f7ff 0%, #eef2ff 35%, #ffffff 100%);
        color: #0d1b2a;
      }

      a {
        color: var(--brand-primary);
        font-weight: 500;
      }

      main.container {
        max-width: 1100px;
        padding: 3rem 1.5rem 4rem;
      }

      main.container > * + * {
        margin-top: 2.5rem;
      }

      .muted {
        color: var(--muted-color);
      }

      .top-bar {
        background: linear-gradient(120deg, rgba(13, 110, 253, 0.95), rgba(102, 16, 242, 0.9));
        color: #fff;
        padding: 0.75rem 0;
        box-shadow: 0 0.75rem 2.5rem rgba(13, 110, 253, 0.25);
      }

      .top-bar__content {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
      }

      .top-bar__brand {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        font-weight: 600;
        font-size: 1.1rem;
      }

      .top-bar__brand img {
        height: 48px;
        width: 48px;
        object-fit: contain;
        border-radius: 12px;
        box-shadow: 0 0.75rem 1.5rem rgba(255, 255, 255, 0.25);
      }

      .top-bar__banner {
        font-size: 0.95rem;
        opacity: 0.95;
      }

      header.site-header {
        background: transparent;
        border-bottom: none;
        margin-bottom: 0;
      }

      header.site-header .container {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 1rem;
        padding: 1.5rem 0 0;
      }

      header.site-header h1 {
        margin: 0;
        font-size: clamp(1.9rem, 3vw, 2.3rem);
        color: #0d1b2a;
      }

      header.site-header nav {
        margin-left: auto;
      }

      header.site-header nav ul {
        display: flex;
        gap: 1rem;
        align-items: center;
        margin: 0;
        padding: 0;
        list-style: none;
      }

      header.site-header nav a {
        color: #0d1b2a;
        font-weight: 600;
        padding: 0.35rem 0.75rem;
        border-radius: 999px;
        transition: background 0.15s ease;
      }

      header.site-header nav a:hover {
        background: rgba(13, 110, 253, 0.12);
      }

      header.site-header nav ul li:last-child a {
        background: rgba(13, 110, 253, 0.12);
        color: var(--brand-primary);
      }

      .page-hero {
        position: relative;
        overflow: hidden;
        background: radial-gradient(circle at top left, rgba(13, 110, 253, 0.18), transparent 60%),
          radial-gradient(circle at bottom right, rgba(102, 16, 242, 0.15), transparent 55%),
          #ffffff;
        border-radius: 1.75rem;
        padding: 2.5rem clamp(1.5rem, 4vw, 2.75rem);
        box-shadow: var(--surface-shadow);
        display: flex;
        flex-wrap: wrap;
        gap: 1.5rem;
        justify-content: space-between;
        align-items: flex-start;
      }

      .page-hero--compact {
        padding: 2rem clamp(1.5rem, 4vw, 2.5rem);
      }

      .page-hero__content {
        max-width: min(640px, 100%);
      }

      .page-hero__content h2 {
        margin: 0 0 0.5rem;
        font-size: clamp(2rem, 3.5vw, 2.6rem);
      }

      .page-hero__content p {
        margin: 0;
        font-size: 1.05rem;
        line-height: 1.6;
        color: var(--muted-color);
      }

      .page-hero__actions {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: flex-end;
        gap: 0.75rem;
      }

      .surface-card {
        background: var(--surface-color);
        border-radius: 1.5rem;
        border: 1px solid var(--surface-border);
        box-shadow: var(--surface-shadow);
        padding: 1.75rem clamp(1.25rem, 3vw, 2rem);
        display: grid;
        gap: 1.5rem;
      }

      .surface-card--stacked {
        gap: 2rem;
      }

      .section-heading {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 1rem;
      }

      .section-heading h3 {
        margin: 0;
        font-size: 1.35rem;
        color: #0d1b2a;
      }

      .status-pill {
        border-radius: 999px;
        padding: 0.35rem 0.85rem;
        font-size: 0.85rem;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        background: rgba(13, 110, 253, 0.08);
        color: var(--brand-primary);
        font-weight: 600;
      }

      .status-pill--success {
        background: rgba(25, 135, 84, 0.12);
        color: #157347;
      }

      .status-badge {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.25rem 0.75rem;
        border-radius: 999px;
        font-size: 0.8rem;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        font-weight: 600;
      }

      .status-badge--success {
        background: rgba(25, 135, 84, 0.12);
        color: #157347;
      }

      .status-badge--muted {
        background: rgba(108, 117, 125, 0.15);
        color: #6c757d;
      }

      .status-badge--warn {
        background: rgba(255, 193, 7, 0.2);
        color: #ad6800;
      }

      .status-badge--error {
        background: rgba(220, 53, 69, 0.18);
        color: #b02a37;
      }

      .table-card {
        border-radius: 1.25rem;
        overflow: hidden;
        border: 1px solid var(--surface-border);
        background: var(--surface-color);
        box-shadow: var(--surface-shadow);
      }

      .table-card table {
        width: 100%;
        border-collapse: collapse;
        background: transparent;
      }

      .table-card thead th {
        background: rgba(13, 110, 253, 0.08);
        color: #0d1b2a;
        font-size: 0.85rem;
        letter-spacing: 0.05em;
        text-transform: uppercase;
      }

      .table-card th,
      .table-card td {
        padding: 0.85rem 1.25rem;
        text-align: left;
        border-bottom: 1px solid rgba(13, 110, 253, 0.08);
      }

      .table-card tbody tr:last-child th,
      .table-card tbody tr:last-child td {
        border-bottom: none;
      }

      .table-card tbody tr:hover {
        background: rgba(13, 110, 253, 0.04);
      }

      .table-card .empty-state {
        text-align: center;
        padding: 2.5rem 1rem;
        color: var(--muted-color);
      }

      .table-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        justify-content: flex-start;
        align-items: stretch;
      }

      .table-actions > * {
        flex-shrink: 0;
      }

      .table-actions form {
        margin: 0;
        display: inline-flex;
        align-items: center;
      }

      .table-actions .button,
      .table-actions button,
      .table-actions a.secondary {
        padding: 0.45rem 1rem;
        box-shadow: none;
        min-width: 120px;
        justify-content: center;
      }

      .form-stack {
        display: grid;
        gap: 1.75rem;
      }

      .form-grid {
        display: grid;
        gap: 1rem;
      }

      .form-grid--two {
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      }

      .form-grid--three {
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      }

      fieldset {
        border: 1px solid rgba(13, 110, 253, 0.15);
        border-radius: 1.25rem;
        padding: 1.5rem;
        background: rgba(13, 110, 253, 0.04);
        display: grid;
        gap: 1rem;
      }

      legend {
        font-weight: 600;
        padding: 0 0.5rem;
      }

      details {
        border: 1px solid rgba(13, 110, 253, 0.15);
        border-radius: 1.25rem;
        padding: 1rem 1.25rem;
        background: rgba(13, 110, 253, 0.04);
      }

      details summary {
        font-weight: 600;
        cursor: pointer;
      }

      label.checkbox {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem 1rem;
        border-radius: 1rem;
        background: rgba(13, 110, 253, 0.06);
        cursor: pointer;
        user-select: none;
      }

      label.checkbox input[type='checkbox'] {
        width: 1.1rem;
        height: 1.1rem;
        flex-shrink: 0;
        accent-color: var(--brand-primary);
        border-radius: 0.35rem;
      }

      .endpoint-toggle,
      .checkbox.toggle {
        align-items: center;
        gap: 0.9rem;
      }

      .endpoint-toggle input[type='checkbox'],
      .checkbox.toggle input[type='checkbox'] {
        position: absolute;
        opacity: 0;
        width: 1px;
        height: 1px;
        margin: 0;
      }

      .endpoint-toggle-track,
      .toggle-track {
        position: relative;
        width: 50px;
        height: 28px;
        display: inline-flex;
        align-items: center;
        padding: 4px;
        border-radius: 999px;
        background: #dce6ff;
        border: 1px solid rgba(13, 110, 253, 0.3);
        transition: background 0.15s ease, border-color 0.15s ease;
        flex-shrink: 0;
      }

      .endpoint-toggle-thumb,
      .toggle-thumb {
        width: 18px;
        height: 18px;
        border-radius: 50%;
        background: #fff;
        box-shadow: 0 4px 10px rgba(13, 110, 253, 0.25);
        transition: transform 0.15s ease, background 0.15s ease, box-shadow 0.15s ease;
      }

      .endpoint-toggle input[type='checkbox']:checked + .endpoint-toggle-track,
      .checkbox.toggle input[type='checkbox']:checked + .toggle-track {
        background: linear-gradient(135deg, var(--brand-primary), var(--brand-secondary));
        border-color: rgba(13, 110, 253, 0.35);
      }

      .endpoint-toggle input[type='checkbox']:checked + .endpoint-toggle-track .endpoint-toggle-thumb,
      .checkbox.toggle input[type='checkbox']:checked + .toggle-track .toggle-thumb {
        transform: translateX(20px);
        box-shadow: 0 4px 12px rgba(102, 16, 242, 0.3);
      }

      .endpoint-toggle input[type='checkbox']:focus-visible + .endpoint-toggle-track,
      .checkbox.toggle input[type='checkbox']:focus-visible + .toggle-track {
        outline: 2px solid rgba(13, 110, 253, 0.6);
        outline-offset: 3px;
      }

      .endpoint-toggle-label,
      .toggle-label {
        font-weight: 600;
      }

      .checkbox-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 0.75rem;
      }

      .checkbox-card {
        border: 1px solid var(--muted-border-color);
        background: rgba(13, 110, 253, 0.04);
        transition: border-color 0.15s ease, background 0.15s ease, box-shadow 0.15s ease;
      }

      .checkbox-card[data-selected='true'] {
        border-color: var(--brand-primary);
        background: rgba(13, 110, 253, 0.12);
        box-shadow: 0 0 0 2px rgba(13, 110, 253, 0.15);
      }

      .checkbox-card input[type='checkbox'] {
        margin-top: 0.2rem;
      }

      .checkbox-card__body {
        display: grid;
        gap: 0.35rem;
      }

      .checkbox-card__title {
        display: flex;
        flex-wrap: wrap;
        gap: 0.35rem;
        align-items: center;
        font-weight: 600;
      }

      .form-error {
        color: var(--danger-color);
        font-size: 0.9rem;
        margin: 0;
      }

      input,
      select,
      textarea {
        border-radius: 0.85rem;
        border: 1px solid rgba(13, 110, 253, 0.25);
        background: rgba(255, 255, 255, 0.96);
        padding: 0.65rem 0.85rem;
        box-shadow: inset 0 0.5rem 1.2rem rgba(13, 110, 253, 0.05);
      }

      textarea {
        resize: vertical;
        min-height: 140px;
        font-family: 'JetBrains Mono', 'Fira Code', monospace;
        font-size: 0.95rem;
        line-height: 1.5;
      }

      pre {
        background: #0d1b2a;
        color: #f8f9ff;
        border-radius: 1.25rem;
        padding: 1.5rem;
        overflow-x: auto;
        box-shadow: 0 1.75rem 3.25rem rgba(13, 110, 253, 0.18);
      }

      pre code {
        background: transparent;
        padding: 0;
        color: inherit;
      }

      code {
        background: rgba(13, 110, 253, 0.08);
        color: #0d1b2a;
        padding: 0.25rem 0.55rem;
        border-radius: 0.75rem;
        font-size: 0.85rem;
        font-family: 'JetBrains Mono', 'Fira Code', monospace;
      }

      .flash {
        border-radius: 0.95rem;
        padding: 0.85rem 1.25rem;
        margin: 0;
        font-weight: 600;
        box-shadow: 0 0.75rem 2rem rgba(13, 110, 253, 0.1);
      }

      .flash + .flash {
        margin-top: 0.75rem;
      }

      .flash-success {
        background: rgba(25, 135, 84, 0.15);
        color: #0f5132;
      }

      .flash-error {
        background: rgba(220, 53, 69, 0.15);
        color: #842029;
      }

      .flash-info {
        background: rgba(13, 202, 240, 0.18);
        color: #055160;
      }

      button,
      .button,
      a.button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.4rem;
        padding: 0.65rem 1.4rem;
        border-radius: 999px;
        border: none;
        background: linear-gradient(135deg, var(--brand-primary), var(--brand-secondary));
        color: #fff !important;
        font-weight: 600;
        letter-spacing: 0.01em;
        box-shadow: 0 1rem 2.5rem rgba(13, 110, 253, 0.25);
        transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s ease;
        text-decoration: none;
        white-space: nowrap;
      }

      button:hover,
      .button:hover,
      a.button:hover {
        transform: translateY(-1px);
        box-shadow: 0 1.4rem 3rem rgba(13, 110, 253, 0.3);
      }

      button:focus,
      .button:focus,
      a.button:focus {
        outline: 3px solid rgba(13, 110, 253, 0.3);
        outline-offset: 2px;
      }

      button.secondary,
      .button.secondary,
      a.button.secondary {
        background: rgba(255, 255, 255, 0.92);
        color: var(--brand-primary) !important;
        border: 1px solid rgba(13, 110, 253, 0.3);
        box-shadow: 0 0.9rem 2.2rem rgba(13, 110, 253, 0.18);
      }

      a.secondary {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.4rem;
        padding: 0.6rem 1.3rem;
        border-radius: 999px;
        background: rgba(13, 110, 253, 0.12);
        color: var(--brand-primary);
        font-weight: 600;
        text-decoration: none;
        box-shadow: 0 0.85rem 2rem rgba(13, 110, 253, 0.12);
        transition: background 0.15s ease, transform 0.15s ease;
        white-space: nowrap;
      }

      a.secondary:hover {
        background: rgba(13, 110, 253, 0.18);
        transform: translateY(-1px);
      }

      .button.contrast,
      button.contrast,
      a.button.contrast {
        background: rgba(255, 255, 255, 0.95);
        color: var(--danger-color) !important;
        border: 1px solid rgba(176, 42, 55, 0.35);
        box-shadow: 0 0.85rem 2rem rgba(176, 42, 55, 0.18);
      }

      .button.button-outline,
      a.button.button-outline {
        background: transparent;
        color: var(--brand-primary) !important;
        border: 1px solid rgba(13, 110, 253, 0.35);
        box-shadow: none;
      }

      .button.button-outline:hover,
      a.button.button-outline:hover {
        background: rgba(13, 110, 253, 0.08);
      }

      .link-arrow {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        font-weight: 600;
        color: var(--brand-primary);
        text-decoration: none;
      }

      .link-arrow::after {
        content: '→';
        font-size: 0.9rem;
      }

      .tag {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border-radius: 999px;
        padding: 0.25rem 0.75rem;
        font-size: 0.85rem;
        font-weight: 600;
        background: rgba(13, 110, 253, 0.08);
        color: var(--brand-primary);
      }

      .tag.success {
        background: rgba(25, 135, 84, 0.12);
        color: #157347;
      }

      .stack {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      .stack--lg {
        gap: 1.75rem;
      }

      .stack--xl {
        gap: 2.25rem;
      }

      .flash + .surface-card,
      .surface-card + .flash {
        margin-top: 1.5rem;
      }

      .action-buttons {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        justify-content: flex-start;
        align-items: center;
      }

      .action-buttons > * {
        flex-shrink: 0;
      }

      .action-buttons .button,
      .action-buttons button,
      .action-buttons a.secondary {
        min-width: 120px;
        justify-content: center;
      }

      .action-buttons form {
        margin: 0;
      }

      .config-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
      }

      .config-field {
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
      }

      .config-field--wide {
        grid-column: span 2;
      }

      @media (max-width: 720px) {
        .config-field--wide {
          grid-column: span 1;
        }
      }

      .config-field--stacked {
        margin-bottom: 0;
      }

      .config-label {
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--muted-color);
      }

      .config-value {
        font-size: 1rem;
        word-break: break-all;
      }

      .config-headers {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
      }

      @media (max-width: 720px) {
        .page-hero {
          padding: 2.25rem 1.5rem;
        }

        header.site-header .container {
          justify-content: center;
        }

        header.site-header nav {
          margin-left: 0;
        }

        header.site-header nav ul {
          justify-content: center;
        }

        .page-hero__actions {
          width: 100%;
          justify-content: flex-start;
        }

        .surface-card {
          padding: 1.5rem;
        }

        .table-card {
          border-radius: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <% const layoutAdminKey = typeof adminKey !== 'undefined' ? adminKey : null; %>
    <div class="top-bar">
      <div class="container top-bar__content">
        <div class="top-bar__brand">
          <img src="/public/logo.svg" alt="Brillar API Tool logo" />
          <span>Brillar API Tool</span>
        </div>
        <div class="top-bar__banner">Design, mock, and test your API endpoints faster.</div>
      </div>
    </div>
    <header class="site-header">
      <div class="container">
        <h1><%= title || 'GUI Mock API' %></h1>
        <nav>
          <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/admin<%= layoutAdminKey ? `?key=${encodeURIComponent(layoutAdminKey)}` : '' %>">Routes</a></li>
            <li><a href="/admin/mcp<%= layoutAdminKey ? `?key=${encodeURIComponent(layoutAdminKey)}` : '' %>">MCP Servers</a></li>
            <li><a class="button" href="/admin/new<%= layoutAdminKey ? `?key=${encodeURIComponent(layoutAdminKey)}` : '' %>">Create Endpoint</a></li>
          </ul>
        </nav>
      </div>
    </header>
    <main class="container">
      <%- body %>
    </main>
    <script src="/public/admin.js" defer></script>
    <% if (typeof scripts !== 'undefined' && scripts) { %>
      <%- scripts %>
    <% } %>
  </body>
</html>
