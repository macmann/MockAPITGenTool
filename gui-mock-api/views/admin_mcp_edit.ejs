<%
  const title = s.id ? 'Edit MCP Server' : 'New MCP Server';
  const body = include('partials/admin_mcp_edit_body', {
    s,
    query,
  });
%>
<%- include('layout', { title, body, adminKey: query?.key }) %>
