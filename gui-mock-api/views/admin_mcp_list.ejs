<%
  const title = 'MCP Servers';
  const body = include('partials/admin_mcp_list_body', {
    servers,
    statuses,
    query,
    statusMessage,
    statusType,
  });
%>
<%- include('layout', { title, body, adminKey: query?.key }) %>
