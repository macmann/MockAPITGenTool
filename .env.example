# Copy this file to `.env` (for local dev) or configure the same values in your hosting provider.
# Local development defaults use SQLite for Prisma and in-memory mocks. Production should set Postgres + real secrets.

# --- Application basics ---
# Port used by Next.js and the legacy mock API server. Vercel/Render/Railway will inject their own PORT.
PORT=3000

# Base URL used by Auth.js / NextAuth. For production, set this to your deployed URL (e.g., https://your-app.vercel.app).
NEXTAUTH_URL=http://localhost:3000
# Generate a strong random string for production (e.g., `openssl rand -hex 32`).
NEXTAUTH_SECRET=changeme-in-production

# --- Database ---
# Local dev (SQLite by default). Leave `DATABASE_PROVIDER` unset or "sqlite" and keep the
# default file-based URL. When running against Postgres, set `DATABASE_PROVIDER` to
# "postgresql" and update `DATABASE_URL` to your connection string.
DATABASE_PROVIDER="sqlite"
DATABASE_URL="file:./prisma/dev.db"
# Production (Render / cloud Postgres)
# DATABASE_PROVIDER="postgresql"
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?schema=public"

# --- Authentication providers ---
# Optional: GitHub OAuth if you want social login alongside credentials.
GITHUB_ID=
GITHUB_SECRET=

# --- Admin bootstrap ---
# Default credentials for the seeded admin account (used by Auth.js credentials provider).
ADMIN_DEFAULT_EMAIL=admin@example.com
ADMIN_DEFAULT_PASSWORD=password
# Allow the dev default admin login in production by setting this to 'true'. Leave
# unset/false in production to force real credentials only.
ADMIN_DEFAULT_ENABLED=false

# --- Mock API / MCP bridge settings ---
# Secret to access the legacy mock API admin dashboard.
ADMIN_KEY=
# SQLite filename for the mock API dashboard (separate from Prisma database).
DB_FILE=mockapis.db
# Default MCP slug and public base URL for the MCP bridge (optional).
MCP_DEFAULT_SLUG=
MCP_PUBLIC_URL=
# Base URL for generated mock API endpoints (optional convenience for MCP clients).
MOCK_BASE_URL=

# --- OpenAI generator (CLI and MCP adapters) ---
OPENAI_API_KEY=
# Default model for the generator; override to match your account.
OPENAI_MODEL=gpt-4o-mini
